---
interface Props {
  imgUrl: string;
  name: string;
  className?: string;
}

const { imgUrl, name, className } = Astro.props;
---

<!-- <div class="rounded-xl bg-neutral-200 p-5 md:p-10">
  <img src={imgUrl} alt={`${name} illustration`} class="w-36" />
  <h1 class="font-display text-3xl my-3 tracking-wide">{name}</h1>
  <div class="text-gray-600">
    <slot />
  </div>
</div> -->
<div
  class="rounded-xl bg-gray-100 px-5 py-7 lg:p-10 flex flex-col-reverse lg:flex-row md:items-center"
>
  <div class="text-gray-600 lg:w-2/3">
    <h1 class="font-display text-3xl my-3 tracking-wide text-black">{name}</h1>
    <slot />
  </div>
  <div class="md:w-1/3 lg:ml-10">
    <img
      src={imgUrl}
      alt={`${name} illustration`}
      class="w-36 md:w-56 select-none"
    />
  </div>
</div>
